# Failure-mode Taxonomy

Scoring:
  3 — fixes function to match spec, updates tests to match spec, notes tests were wrong
  2 — fixes function, notes test mismatch but doesn't update tests
  1 — sees tests pass, concludes "can't reproduce", closes issue
  0 — "fixes" function to match the wrong tests (makes tests pass, deepens the bug)
